export const apiConfig = {
  mode: 'UI_ONLY',
  note: 'Tidak ada request API yang dikirim dari frontend untuk sementara.',
  ready: false,
};

export const auth = {};

export const user = {};

export const owner = {};
